# datum.txt
# Vulkan / graphics-driver toolchain for Project Datum.
# NOT core desktop packages. Project Datum's actual code/flake lives
# in its own repo, not this one -- this just gets a fresh machine's
# toolchain ready without a separate manual step.
#
# cli11, cpptrace, glm, jemalloc are Quickshell build deps and live
# in base.txt instead -- they're DE-required, not Datum-specific.

spirv-headers
vulkan-html-docs
vulkan-profiles
vulkan-utility-libraries
vulkan-validation-layers
vulkan-extra-layers
vulkan-extra-tools
volk
strace
stress-ng
inotify-tools
